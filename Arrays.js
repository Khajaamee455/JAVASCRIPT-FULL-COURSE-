// Arrays in javascript 

const languages =["java" , "python", "javascript", "c++"];
// this is an array ...

// With different datatypes ...

const data = [1 , "khaja" , true ];
// with differrnt datatypes 

const numbers =[1,2,3,4];
numbers.length();  // 4


//Aceesing elements with their indexes .....

const myarray = [100, 200, 300 , 400 ,500];
console.log(myarray[0]);
console.log(myarray[3]);

// Some array methods .....

const games= ["pubg", "bgmi", "callofduty", "minimitia"];

games.push('GTA5')          // add to end of the array 
games.pop();             // remove from the end of array       
games.unshift();       // add starting of the array 
games.shift();    // remove starting from array
games.concat(); // use to add in existing array

