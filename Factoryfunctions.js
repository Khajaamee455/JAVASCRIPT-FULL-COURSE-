// A factory function that accepts 'name', 
// 'age', and 'language' parameters to return 
// a customized user object. 


const user = (name, age, language) => {
    return {
      name: 'khaja',
      age: 24,
      language : 'kannada',
     javascrit(){
       console.log("javascript is best language..")
     }
    };
  };

  
  